# ping <url>

# sudp apt install net-tools
# ifconfig
# tcpdump

# netstat

# sudo apt-get install traceroute
# traceroute

# ssh
# sftp

# network status
netstat
netstat -tupln
sudo netstat -tupln # for more info 

# Generate SSH key --
ssh-keygen -t rsa

# ssh into remote host
ssh user@host_address -p <port_no> # ssh default port is 20