-- INNER JOIN
-- Fetches matching records only

-- LEFT JOIN
-- INNER JOIN + rest records from left table

-- RIGHT JOIN
-- INNER JOIN + rest records from right table

-- FULL OUTER JOIN/FULL JOIN
-- INNER JOIN 
--  + all remaining records from left table (left join)
--  + all remaining records from right table (right join)

-- CROSS JOIN/CARTESIAN JOIN
-- returns cartesian product

-- SELF JOIN
-- inner join on same table

-- NATURAL JOIN

 
